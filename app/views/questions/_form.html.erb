<%# This file is considered to be a partial which means you can include it in any other template (like new or edit), you must start the partial name with _ . Partials have access to the same instance variables available in the templates (e.g. @question) %>

<%= @question.errors.full_messages.join(". ") %>
<br>
<br>


<%# form_for is a Rails helper method %>
<div class="row">
  <div class="col-lg-6 col-md-6 col-sm-8 col-xs-12">
      <%= simple_form_for @question do |f| %>
      <%# if @question is not persisted in the database. form_for makes the 'action' of the form go to 'questions_path' and it will make the method of form to be POST%>
        <div>
          <%= f.input :title %>
          <%= f.input :body %>
          <%= f.association :category %>
          <%= f.association :tags, input_html: {class: "chosen-select"} %>
          <%= f.input :image %>
          <%# Byusing f.text_field :title, if @question has a value for the attribute 'title' it will populate the field with that value. This is usually the case when redisplaying the form after an error occurs %>
        <!-- <div> -->
          <%# f.label :category %>
          <%# f.collection_select :category_id, Category.order(:name), :id, :name, {prompt: "Select a category"}, {class: "form-control"} %>
        <!-- </div> -->
      <br>
        <!-- <div style="display: inline-block; margin-bottom: 10px"> -->
          <%# f.label :tag_ids, "Tags" %>
          <!-- <br> -->
          <%# f.collection_check_boxes :tag_ids, Tag.order(:name), :id, :name do |c| %>
            <!-- <span class="checkbox-s col-md-4 col-sm-4 col-xs-6"> -->
              <%# c.label { c.check_box + c.text } %>
            <!-- </span> -->
          <%# end %>
        </div>
          <div>
            <%= f.submit class:"btn btn-success btn-lg"%>
          </div>

      <% end %>
  </div>
</div>
